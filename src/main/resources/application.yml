server:
  port: 5050
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/develhopetest
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop